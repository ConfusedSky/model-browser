{
  "name": "model-browser",
  "private": true,
  "engines": {
    "node": ">=24.7.0"
  },
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "dev:demo": "MODEL_BROWSER_CONFIG=$PWD/deploy/demo/config.json MODEL_BROWSER_ROOT=${MODEL_BROWSER_ROOT:-$HOME/Documents/tests/test-models/miniatures/decimated} bun run --filter '*' dev",
    "test": "bun run --filter '*' test",
    "typecheck": "bun run --filter '*' typecheck"
  }
}
