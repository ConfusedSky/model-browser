## 1. Start from a clean tree

- [x] 1.1 Remove the probe: delete `client/src/probes.ts`, its import and call in `client/src/main.tsx`, and the `probeOn("virtual")` branch, draft `ROW_CLASS` and `rangeKey` dep in `Grid.tsx`; keep `@tanstack/react-virtual`. Verify `git diff HEAD -- client/src` prints nothing and `git diff HEAD --stat` lists only `client/package.json` and `bun.lock`; `bun run typecheck` passes.
  - 2026-09-25: the probe was never committed; it was removed from the main checkout before the proposal commit 762e0a6, which carries only the dependency (`client/package.json`, `bun.lock`) besides the planning files.

## 2. The geometry seam and the virtual grid (D1, D2, D3, D10, D11, D13)

- [x] 2.1 Add `setGridGeometryForTests({ viewport, rowHeight, cols, gridTop })` feeding TanStack's `observeElementRect`, `observeElementOffset` and `measureElement`, standing in for D2's column read, and installing D13's `getBoundingClientRect` stub (tiles by listing index), which replaces `retracePlacement.test.tsx`'s `installGeometry`; give the app harness a default viewport tall enough for its listings with a fixed `cols`. Lands with 2.3 in one commit, so no commit leaves `tiles()` cells red. Verify the full client suite passes after 2.3 with no cell edited except those this change names, and that nothing switches virtualization off.
- [x] 2.2 Add `ROW_CLASS` (whole literals per size, `GRID_CLASS` less vertical padding, row gap as bottom padding) and the measuring row; derive `cols` from its computed `grid-template-columns` under a `ResizeObserver`; delete `columnCount`. Verify with a unit cell over a fixed track list, and in the browser (6.3) that `cols` equals the tracks a real row displays at 390 px and 1400 px for each tile size.
- [x] 2.3 Render `entries` as rows through `useVirtualizer` over `scrollRoot` with `scrollMargin`, a body of `getTotalSize()` height and rows positioned by `translateY`, on every view the grid draws. Verify under the seam that a 600-entry listing with a two-row viewport mounts only the viewport's rows plus overscan and the first row, and in the browser (6.3) that the tiles in the document on `?flat=1` of the demo corpus stay under 60 at 390×844 while the scroll height spans the listing.
- [x] 2.4 `rangeExtractor`: TanStack's range plus the first row, the last-focused row (recorded on `focusin`, kept until another tile takes focus or the entry leaves `entries`) and the handle's pinned row, sorted. Verify with cells that (a) focus a tile, scroll the geometry far away, and find the same element still connected and focused, and (b) move focus to the search input, scroll away, and find the tile still connected; both fail with the last-focused term removed.
- [x] 2.5 `estimateSize` by row composition with `virtualizer.measure()` the first time each composition is measured (D10). Verify with a unit cell over folders then models: once one row of each kind is measured, `getTotalSize()` equals the sum of the kinds' measured heights over their row counts.
- [x] 2.6 Re-read the scroll margin in the frame callback and on resize (D11). Verify with a cell that moves the seam's `gridTop` down by a row's height and finds the visible range shifted by one row.

  - 2026-09-25, 319e445 (stage A): cells in `test/gridVirtualization.test.tsx` (2.3–2.6, the scrolled-mount cell) and `gridArrowNav.test.tsx` (`trackCount`); falsified by dropping the last-focused term (2.4 a/b), returning every row (2.3), removing `measure()` (2.5), the frame margin read (2.6), `initialOffset` and `initialRect`. Suite on merged main: 87 files, 1159 passed. The browser halves of 2.2 and 2.3 are in 6.3.
## 3. Positions and previews from the layout (D4, D5)

- [x] 3.1 Write `bandsForRows` returning a band for every entry, preview cells one step farther and nearest winning; replace `FAR_ROOT_MARGIN` with `NEAR_SCREENS = 2`. Verify with unit cells: an unmounted near folder is `near`, an unmounted far model is `far` (present in the map), a model shown as a tile and as a visible folder's cell takes the nearer band.
- [x] 3.2 Drive it from the virtualizer — publish on scroll frames, resize, row measurement, `entries` and `previews` changes, only when a range or the listing changed; `onPeek` for folders entering the near range — and remove both `IntersectionObserver`s, `bandStateRef`, the half-heard guard, and the observer prose in `Grid`'s `Props` docs (`scrollRoot`, `onPeek`, `onBands`). In the same commit, port `folderSheets.test.tsx`'s band and peek cells to scrolling the seam's viewport and delete the `reportHalf` cells, giving the reason. Verify the ported cells pass; that with `bandsForRows` answering `visible` for every entry the cells asserting deferral, ranking behind visible work, or no peek yet fail, and the commit message lists which cells were expected to stay green; and that a scroll within one row publishes nothing.
- [x] 3.3 Move `data-offscreen` to rows outside the visible range and update the rule's comment in `index.css`. Verify by replacing the per-tile cell ("marks a tile off screen …") with a row-level one that fails when the attribute is not set, and in the browser (6.3) that a spinner in an overscan row computes `paused` and one in a visible row `running`.

  - 2026-09-25, 8717885 (stage B): `lib/gridBands.ts` + `test/gridBands.test.ts` (12 cells, nearest-wins both ways); folderSheets band/peek cells ported to a short seam viewport, the two `reportHalf` cells deleted; new cells for an unmounted near folder and a within-row scroll. Falsified: `bandsForRows` all-visible (failing/green lists in the commit message), `NEAR_SCREENS = 0`, the row `data-offscreen` prop, and the flat-toggle race by a temporary App edit. Suite on merged main: 88 files, 1172 passed.
- [x] 3.4 Follow-up found by 3.2's mutation: "a far folder's cells wait behind a visible tile" stays green when every row reads visible, because a visible folder's cells rank near, still behind the visible tile — it cannot tell far from near, and could not before this change either. Add a near model tile to the fixture that the far folder's cells must also wait behind. Verify the cell fails under the all-visible mutation.
  - 2026-09-25, 8b9c6a9: 3.4's fixture gains a near model tile and a visible folder with its own cell; fails under the all-visible row mutation on the visible folder's cell.
## 4. The handle, keys and focus (D6, D7, D8, D9, D12)

- [x] 4.1 Add the `handle` ref: `place(resolved)` (pin, raw `scrollTop` write to the row's offset, `applyIn` in the layout effect after the row mounts; never `scrollToIndex`/`scrollToOffset`) and `focusEntry(target, options)` (pin, `focus(options)` after mount; false when not shown); both inert when no grid is mounted. Verify with cells for an anchor, a centre and a focus target that each start unmounted, the anchor landing at its offset to the pixel under the seam.
- [x] 4.2 Move App's call sites per D6's table, keeping the weak set's and `closeFind`'s deferrals, and drop App's `findTile`/`tilesIn` imports. Verify `retracePlacement.test.tsx` and the focus cells pass unchanged, plus new cells — back to a far anchor, reveal far down, going up to a far folder — whose anchors start unmounted.
- [x] 4.3 Arrows and Tab as index arithmetic through `focusEntry` (D7). Verify `gridArrowNav.test.tsx` passes with its rect stubs replaced by the seam's `cols`; a cell pressing ArrowDown from row 0 past the viewport visits every row; and a cell that focuses a tile, scrolls the geometry away and dispatches Tab then Shift+Tab lands on the next tile then back, failing when the Tab handling is removed.
- [x] 4.4 Lightbox close by entry with `returnsFocus` (D8); `navigateSibling` stops looking the tile up. Verify cells: open from a tile, step past the mounted rows, close — focus on the stepped-to tile; open by deep link and close without stepping — focus not moved.
- [x] 4.5 Keep the top entry across a column change with `virtualizer.measure()` before the re-anchor (D9). Verify a cell that scrolls a long listing, changes `size` from `m` to `s` (seam `cols` 3 → 5), and finds the same entry first in the top visible row at the same offset.
- [x] 4.6 `ThumbView` counts an already-complete image as loaded on mount (D12). Verify a cell whose image reports `complete` shows no spinner and one whose image does not shows it; the lazy-image assumption is checked in 6.3.

  - 2026-09-25, c78cbfa (stage C): `test/gridHandle.test.tsx` (12 App-level cells: far anchor to the pixel, far reveal, ↑ to a far folder, Tab/Shift+Tab after scrolling away and at the ends, ArrowDown through every row, lightbox stepped far then closed, deep link unstepped and stepped once, m→s, no grid) and 6 Grid-level cells in `gridVirtualization.test.tsx` (handle anchor/centre/focus, the missed-scroll bound, ThumbView complete vs loading). Falsified: Tab handling, the step rule, `returnsFocus` both ways, the D9 re-anchor, phase 1 only, skip `applyIn`, the D12 check, the frame loop and its cap. `originEl` replaced by `returnsFocus` (fixtures in viewerLayer/orbitHandoff tests). Suite on merged main: 89 files, 1189 passed.
## 5. Bench

- [x] 5.1 Add `scripts/scroll-bench.mjs` (D14) and a line for it in CLAUDE.md under `preview:remote-demo`. Verify it runs headless against `bun run preview:remote-demo` and prints the metrics D14 names.
  - 2026-09-25, 09155dc: two runs on `?flat=1` against the pre-change production build on 3177 (library 70b60f0d, the demo corpus): 461–462 frames, p95 50 ms, 88–95 frames over 33 ms, main 5.9 s, 954 tiles in the document.
- [x] 5.2 Bench the change against the probe's numbers in proposal.md on the demo corpus, root and `?flat=1`, two runs each, and paste the table into design.md under D14. Verify the flat view's frames over 33 ms and main-thread time are no worse than the probe's by more than the spread between the two runs.
  - 2026-09-25, 5d43a35: table in design.md D14. Flat 15 of 728 over 33 ms and 2.5 s against the probe's 13 of 730 and 2.4 s, within the spread of the bench's two runs (9 and 16); folders better than the probe.

## 6. Verification

- [x] 6.1 `bun run typecheck`, `bun run test` and `bun run format:check` pass; `openspec validate grid-virtualization` passes.
  - 2026-09-25: both workspaces typecheck; client 89 files / 1189 passed, server 31 files / 950 passed; format:check clean; validate clean.
- [x] 6.2 Grep the tests for every scenario of the two delta specs and list, in this line's note, the cell that covers each or "6.3 only".
  - 2026-09-25 (at 5d43a35): cells found for every scenario but one; partial ones leave the real-layout half to 6.3.
    - *keeps a screenful* → gridVirtualization "mounts only the rows near the view …"; *scrolling brings tiles in* → same cell for drawing, presses/menu 6.3 only, hover nowhere;
    - *back far* → gridHandle "Back to a place far down …"; *reveal far* → gridHandle "a reveal far down a folder …" (identity case: 6.3, see 7.1);
    - *ArrowDown* → gridHandle "ArrowDown from the first row visits every row"; *Tabbing through* → partial (Tab/Shift+Tab after scrolling away, ends), repeated Tab 6.3 only;
    - *focus not lost* → gridVirtualization "stays in the document, focused …"; *Tab after scrolling away* → gridHandle; *Escape from the path bar* → mechanism only (gridVirtualization, via the search input), end to end 6.3;
    - *lightbox stepped far* → gridHandle "a close after stepping past the drawn rows …"; *up to a far folder* → gridHandle; *resize keeps the top entry* → gridHandle m→s (tile size), window resize 6.3;
    - model-thumbnails: *below the drawn rows is near* → gridVirtualization "ranks and peeks a folder below …" + gridBands; *far, not unreported* → gridBands + the same cell; *scrolling re-ranks never-drawn tiles* → no direct cell (gridBands `layoutRanges`, the within-row publish cell's control), 6.3 cannot show render order on a baked, write-refusing deployment.
- [x] 6.3 Drive the production build in headless Playwright: back to a far anchor, reveal far down, ArrowDown and Tab past the first screen, Tab into the grid from the header, Escape from the path bar after scrolling, lightbox stepped far then closed, resize and tile-size change, folder previews and thumbnails filling below the first screen, D2's column count, 2.3's mounted-tile bound, 3.3's computed play states, and D12's `complete` on a remounted lazy image. Verify each by measurement (`getBoundingClientRect`, `document.activeElement`, computed style), not by screenshot.
  - 2026-09-25 (at 5d43a35, library 70b60f0d, headless Chromium, 390×844 touch and 1400×1000): 10 of 12 pass — mounted bound (12–22 tiles at 390, 77 at 1400), columns per size at both widths, back to a far anchor (Δ 0 px, 4/4), ArrowDown and Tab ×40 in order, Tab from the header, Escape from the path bar after scrolling, lightbox stepped 31/50 past the mounted rows then closed, previews and images filling at 50 %, paused/running play states by row, remounted lazy images `complete` at mount (D12's assumption confirmed). FAILED: reveal far down (marked, not centred: the pending place is dropped when a follow-up listing re-creates `entries`) → 7.1; the top entry drifting across successive column changes → 7.2. Re-run both after the fix round.
- [ ] 6.4 Hand the owner the tailnet preview for a phone check of a long flat view: momentum fling, scroll back up, return to a folder. Leave this line open until the owner has judged it.

## 7. Fix rounds from review and verification

- [x] 7.1 A pending landing survives a new `entries` array with the target still in it (reveal lost its centring when a follow-up listing re-created the array). Verify with a cell that re-renders with a same-content array before the landing completes, falsified by restoring the identity check, and re-run 6.3's reveal item.
- [x] 7.2 D9 keeps the recorded entry while it stays in the top visible row, so successive column changes do not drift. Verify with a two-step column change cell, and re-run 6.3's resize item.
- [x] 7.3 Implementation review pass 1: the landing waits for every visible row to be measured and for any pending D10 `measure()`; the seam gains `scrollTiming: "production"` with a fake `ResizeObserver`; `recordTop` runs before the margin early return; the handle's empty-state doc; the no-grid fallback cell; "stage" wording out of code comments. Verify with the production-timing far-anchor cell (falsified by removing the wait) and the fallback cell.
  - 2026-09-25, aa25849 + d3f4ce7: 7.1 and 7.3 landed with falsified cells (production-timing far anchor 19200 vs 24300 without the wait, and converging in 2 of 10 frames; follow-up array keeps the centre 0 vs 16490; margin-frame top 12250 vs 18050). Browser re-run of 6.3's reveal: 20 of 20 runs centred within 0.13 px and marked. Implementation review pass 2: READY TO DEPLOY (three minors, fixed in d3f4ce7). 7.2 half-fixed: the entry no longer wanders on the first entry of the row, but the browser re-run fails when the top row is thin — see 7.4.
- [x] 7.4 The kept top position carries as a fraction of its row, and a row less than half shown yields the top to the next one; diagnose and fix the reproducible ten-row jump on 1150 → s → 900 with a thin top row. Verify with seam cells falsified by pixel offsets, and a browser re-run across 2/12/30/60/120 px showing.
  - 2026-09-25, bb57b03 (fix round 2): the jump was the landing loop's no-op writes — an anchor offset past its row's estimated height put the row just above the view, each pass rewrote the same `scrollTop` (no `scroll`, no measurement), four tries burned in one task, and the landing timed out onto estimates that D10 then corrected under it. The loop now waits once the view is at the row's target; D9 carries a fraction of the row and takes the next row when less than half shows. Four cells, falsified (pixel offsets, the reached test, the half-row rule). Browser re-run, `?flat=1` 1400 m → 1150 → l → s → 900: the entry held at every step for 2/12/30/60/120/150/200/240 px showing (fractions within 0.4 %); browser retrace to a thin tile Δ 0 px in 5 of 5. 6.3 is now 12 of 12.
  - 2026-09-25: implementation review pass 3 over d3f4ce7..f440486: READY TO DEPLOY, two minors deferred as 7.5 and 7.6.
- [ ] 7.5 Follow-up (review pass 3, minor): the half-row rule in `recordTop` can yield to a row that is not on screen when a row is taller than twice the viewport (size `l` on a landscape phone); require the next row to be visible (`visible.last > row`) before yielding. Verify with a seam cell whose viewport is under half a row.
- [ ] 7.6 Follow-up (review pass 3, minor): a landing target past the scroll extent is clamped by the browser, so `reached` never holds and the tries burn in one task, as 7.4's mechanism did; compare against `min(target, scrollHeight - clientHeight)`. Bounded today (lands via `applyIn` after `MAX_PLACE_TRIES`); bites only an interleaved search with an anchor in the last screen. Verify with a production-timing cell anchoring in the last screen.
- [ ] 7.7 Owner's phone check (6.4) found rows overlapping on first load on iPhone Safari: rows spaced at `ROW_ESTIMATE.m` (220) while tiles are ~238. Mechanism, from an instrumented build: every composition reset (entries, `cols`, `size`) empties the recorded heights after a `measure()` cleared TanStack's cache, and the rows already mounted are not measured again — their ref is stable and the ResizeObserver fires only on a size change — so recovery hung on an incidental measurement (Chromium's observer delivering its first entries just after the first `cols` reset). Fix: on a reset commit, re-measure the mounted rows through the grid's own `measureElement` and `resizeItem`. Verify with a seam cell (fallback 220, models 250, a reset with no new row) falsified by removing the fix, and a browser run with the follow-up listing delayed; then the owner's phone.
  - 2026-09-25, 87da8f9: fix landed — on the commit where D10's record is replaced, the mounted rows are re-measured through the grid's own `measureElement` and `resizeItem` before D10's `measure()`. Cells (fallback 220 / models 250; a new listing and a column change with no row drawn) fail without it at 0/220/440. Headless Chromium on the demo corpus: rows step by 250 at 300 ms, 2.5 s and 5 s, with the follow-up listing delayed too. Implementation review pass 4: READY TO DEPLOY — the first-load failure was deterministic, not a timing race (the first column read always emptied the record after `measure()`), and the fix covers every reset path. Leave this line open until the owner has checked the iPhone.
- [ ] 7.8 Follow-up (review pass 4, minor): a reset re-measure with no `measure()` before it (a filter keystroke, a follow-up after the observer refilled the cache) takes TanStack's cached size, not the row's, so a row whose composition changed records its old height and undrawn rows keep that estimate until the next reset; on-screen rows are corrected by the observer. In the override, read `el.offsetHeight` when no observer entry is given; the seam never consults the cache, so add a cell that seeds a stale cached size.
- [ ] 7.9 Follow-up (review pass 4, minor): `resizeItem` from the reset effect can reach react-virtual's `flushSync` inside a layout effect (a re-measured row above the fold, not on iOS), which logs a development warning on 5173; behaviour is correct. Decide: accept and say so in the effect's comment, or avoid the synchronous adjustment path.

